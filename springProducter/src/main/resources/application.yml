server:
  port: 8020
  
spring:
  application:
    name: springProducter

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:8761/eureka

logging:
  level:
    root: INFO
